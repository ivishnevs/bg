
import { isActiveGame } from 'ui/helpers/is-active-game';
import { module, test } from 'qunit';

module('Unit | Helper | is active game');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isActiveGame([42]);
  assert.ok(result);
});

