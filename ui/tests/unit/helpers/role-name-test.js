
import { roleName } from 'ui/helpers/role-name';
import { module, test } from 'qunit';

module('Unit | Helper | role name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = roleName([42]);
  assert.ok(result);
});

