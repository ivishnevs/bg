
import { isCurrentGamer } from 'ui/helpers/is-current-gamer';
import { module, test } from 'qunit';

module('Unit | Helper | is current gamer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isCurrentGamer([42]);
  assert.ok(result);
});

